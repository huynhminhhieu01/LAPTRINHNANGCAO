<!DOCTYPE html>
<html lang="vi">

<%- include('partials/head', { title: 'Giỏ hàng' }) %>

<body>
  <main class="container py-5">
    <h1 class="mb-4 text-center">Giỏ hàng của bạn</h1>

    <% if (cart.length === 0) { %>
      <div class="alert alert-warning" role="alert">
        Giỏ hàng của bạn hiện đang trống.
      </div>
    <% } else { %>
      <ul class="list-group">
        <% cart.forEach(item => { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <% if (item.product) { %>
              <div>
                <strong><%= item.product.name %></strong><br>
                <span>Số lượng: <%= item.quantity %></span><br>
                <span>Giá: <%= item.price %> VND</span>
              </div>
              <span class="badge bg-primary rounded-pill">Tổng: <%= item.price * item.quantity %> VND</span>
            <% } else { %>
              <p>Sản phẩm không tìm thấy.</p>
            <% } %>
          </li>
        <% }) %>
      </ul>

      <div class="mt-4 text-end">
        <h3>Tổng giá trị giỏ hàng: <%= totalPrice %> VND</h3>
        <a href="/checkout" class="btn btn-success">Thanh toán</a>
      </div>
    <% } %>

  </main>

  <%- include('partials/footer') %>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
